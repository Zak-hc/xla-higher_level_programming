function changeclr() {
    // Select the element with the ID "myElement"
    var element = document.querySelector("header");

    // Check if the element exists
    if (element) {
        // Change the clr  selected element
        element.style.color == "#FF0000";
